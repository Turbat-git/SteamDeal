<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="SteamDeal.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:SteamDeal"
    xmlns:views="clr-namespace:SteamDeal.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="SteamDeal">

    <TabBar>
        <ShellContent Title="Deals" ContentTemplate="{DataTemplate local:MainPage}" />
        <!-- TODO: Create these pages -->
               <!-- <ShellContent Title="Wishlist" ContentTemplate="{DataTemplate views:WishlistPage}" /> -->
               <!-- <ShellContent Title="Settings" ContentTemplate="{DataTemplate views:SettingsPage}" /> -->
    </TabBar>

    <ShellContent Route="GameDetailPage" ContentTemplate="{DataTemplate views:GameDetailPage}" />
</Shell>